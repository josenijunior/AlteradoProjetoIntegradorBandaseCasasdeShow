<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appdigital.projetointegradorbandasecasasdeshow">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginAll">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".usuario.Usuario_Banda" />
        <activity android:name=".usuario.Usuario_Casa_Show" />
        <activity android:name=".cadastro.Cadastro_Usuario" />
        <activity android:name=".cadastro.Cadastro_De_Novo_Usuario" />
        <activity android:name=".login.ProfileActivity" />
        <activity android:name=".login.Tela_deOpcao" />
        <activity android:name=".cadastro.CadastroDeBandas" />
        <activity android:name=".contrato.Contrato" />
        <activity android:name=".contrato.Notificacao"></activity>
    </application>

</manifest>